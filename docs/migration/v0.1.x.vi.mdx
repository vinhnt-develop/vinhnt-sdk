---
title: "Hướng dẫn nâng cấp v0.1.x"
description: "Hướng dẫn nâng cấp cho các phiên bản v0.1.x"
lang: "vi"
type: "guide"
category: "More"
sidebarPosition: 2
---

# Hướng dẫn nâng cấp — v0.1.x

## Nâng cấp lên v0.1.3

### Thay đổi Breaking

Không có — v0.1.3 tương thích ngược với v0.1.2.

### Deprecated

| Cũ | Mới | Hành động |
|-----|-----|-----------|
| `TokenMeter` (trace) | `CostMeter` | Đổi tên import |
| `findServerByExtension()` | `LspServerRegistry.findByExtension()` | Dùng class method |
| `findServerById()` | `LspServerRegistry.findById()` | Dùng class method |
| `getLanguageId()` | `LspServerRegistry.getLanguageId()` | Dùng class method |

### Tính năng mới

- `CircuitBreakerOpenError` — bắt lỗi circuit breaker
- `ToolTimeoutError` — bắt lỗi timeout
- Tests config cho `parseEnvFile`, `resolveCredentialMultiLayer`
- Tiện ích `approximateTokens` chia sẻ

### Code cần thay đổi

```typescript
// Trước (v0.1.2)
import { TokenMeter } from "@vinhnt-sdk/trace";
const meter = new TokenMeter();

// Sau (v0.1.3)
import { CostMeter } from "@vinhnt-sdk/trace";
const meter = new CostMeter();
```

## Nâng cấp từ v0.1.1 lên v0.1.2

### Packages mới

Thêm 11 packages mới:

- `@vinhnt-sdk/config` — Credential resolution
- `@vinhnt-sdk/llm` — LLM adapter abstraction
- `@vinhnt-sdk/sandbox` — Process isolation
- `@vinhnt-sdk/guard` — Circuit breaker, timeout
- `@vinhnt-sdk/session` — Session state
- `@vinhnt-sdk/permission` — Permission rules
- `@vinhnt-sdk/step-executor` — Execution kernel
- `@vinhnt-sdk/event` — Event bus
- `@vinhnt-sdk/trace` — Observability
- `@vinhnt-sdk/security` — Secret redaction
- `@vinhnt-sdk/mcp` — MCP integration

### Cài packages mới

```bash
pnpm add @vinhnt-sdk/config @vinhnt-sdk/llm @vinhnt-sdk/sandbox \
  @vinhnt-sdk/guard @vinhnt-sdk/session @vinhnt-sdk/permission \
  @vinhnt-sdk/step-executor @vinhnt-sdk/event @vinhnt-sdk/trace \
  @vinhnt-sdk/security @vinhnt-sdk/mcp
```
